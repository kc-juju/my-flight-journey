# The build runs on GitHub runners, which cannot reach Synology's internal
# mirror. Pin the public registry so a local install never writes internal
# URLs into package-lock.json again.
registry=https://registry.npmjs.org/
